import { Barlow_Condensed, Open_Sans } from "next/font/google"

export const open_Sans = Open_Sans({ subsets: ['latin'], weight: ['700'] })
export const barlow = Barlow_Condensed({ subsets: ['latin'], weight: ['400'] })
